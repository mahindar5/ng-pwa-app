# This workflow will do a clean installation of node dependencies, cache/restore them, build the source code and deploy to GitHub Pages
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-nodejs

name: ng-pwa-app-deploy

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read # This is necessary to checkout the repository
  id-token: write # This is necessary to deploy to GitHub Pages
  pages: write # This is necessary to deploy to GitHub Pages

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@main
      - name: Set up Node.js
        uses: actions/setup-node@main
        with:
          node-version: "lts/*"
          cache: "npm"
      - name: Configure npm for publishing
        run: |
          echo "@mahindar5:registry=https://npm.pkg.github.com" >> ~/.npmrc
          echo "//npm.pkg.github.com/:_authToken=${{ secrets.GITHUB_TOKEN }}" >> ~/.npmrc
          echo "registry=https://registry.npmjs.org/" >> ~/.npmrc
      - run: npm ci
      - run: npm run build  -- --configuration=production --output-path=dist --base-href="/ng-pwa-app/"
      - name: Archive production artifacts
        uses: actions/upload-artifact@main
        with:
          name: github-pages
          path: dist/browser

  deploy:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@main
      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@main
